{
  "name": "resumeflame",
  "version": "1.0.0",
  "description": "ResumeFlame — AI-powered resume analyzer (monorepo root)",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"SERVER,CLIENT\" -c \"yellow,cyan\" \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "start:server": "npm start --prefix server",
    "start:client": "npm start --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
